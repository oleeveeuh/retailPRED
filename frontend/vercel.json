{
  "buildCommand": "sh -c 'VITE_DEMO_MODE=true VITE_API_URL= npm run build:prod'",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
